import SwiftUI

// ContentView is replaced by MainTabView — kept for preview compatibility
struct ContentView: View {
    var body: some View {
        MainTabView()
    }
}

#Preview {
    ContentView()
}
